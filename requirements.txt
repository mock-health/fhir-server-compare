# Pinned exactly for reproducibility. Regenerate with:
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
httpx==0.28.1
PyYAML==6.0.1
matplotlib==3.6.3
jsonschema==4.10.3
